services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: tcg_user
      POSTGRES_PASSWORD: tcg_pass
      POSTGRES_DB: tcg_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: